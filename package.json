{
  "name": "pitch-gauge",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "build:js": "esbuild src/main.js --bundle --outfile=src/bundle.js --format=esm --platform=browser",
    "watch:js": "esbuild src/main.js --bundle --outfile=src/bundle.js --format=esm --platform=browser --watch"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "esbuild": "^0.28.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-global-shortcut": "^2",
    "@tauri-apps/plugin-process": "^2.3.1",
    "@tauri-apps/plugin-shell": "^2.3.5",
    "@tauri-apps/plugin-store": "^2",
    "@tauri-apps/plugin-updater": "^2.10.1"
  }
}
